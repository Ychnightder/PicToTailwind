generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}


// model User {
//   id          String       @id @default(uuid())
//   email       String       @unique
//   createdAt   DateTime     @default(now())
//   conversions Conversion[] // Un utilisateur peut avoir plusieurs conversions
// }


// model Conversion {
//   id         String   @id @default(uuid())
//   htmlCode   String   // Le code HTML/Tailwind généré par Groq
//   createdAt  DateTime @default(now())
  
//   // Relation avec la table User
//   userId     String
//   user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
// }